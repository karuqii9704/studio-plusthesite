# --- Client (bundled into the SPA, safe to expose) ---
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
# Where the main marketing site lives (used for payment hand-off + legal links)
VITE_MAIN_SITE_URL=https://www.plusthe.site

# --- Server only (never bundled; read by server/index.js on the VPS) ---
GEMINI_API_KEY=your-gemini-key
PORT=8787
# Comma-separated origins allowed to call the AI proxy in production
ALLOWED_ORIGINS=https://studio.plusthe.site
